name: refreshables
description: Refreshable utilities.
publish_to: none
version: 1.0.0+1
environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_staggered_grid_view: ^0.4.0
  pull_to_refresh: ^2.0.0

dev_dependencies:
  test: ^1.17.9
