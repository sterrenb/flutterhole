name: sterrenburg.github.flutterhole
description: A third party Android application for the Pi-hole® dashboard
version: 1.0.0+1

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  http: ^0.12.0+1
  quick_actions: ^0.2.2
  shared_preferences: ^0.4.3
  fluttertoast: ^2.2.7
  qrcode_reader: ^0.4.4
  url_launcher: ^4.1.0+1
  package_info: ^0.4.0
  dynamic_theme: ^1.0.0
  mockito: ^4.0.0
  launch_review: ^1.0.1
  flutter_markdown: ^0.2.0
  flutter_picker: ^1.0.7
  logging: ^0.11.3+2
  page_view_indicator: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.5.1
  flutter_launcher_icons: ^0.7.0
  screenshots: ^0.1.4

flutter_icons:
  android: "launcher_icon"
  image_path: "assets/icon/icon.png"

flutter:
  uses-material-design: true
  assets:
  - assets/icon/icon.png